<!DOCTYPE html>
<html lang="fa">
<head>
    <meta charset="UTF-8">
    <title>روانشناس هوش مصنوعی (همه شاخه‌ها)</title>
    <style>
        body { font-family: Tahoma; direction: rtl; background: #f5f5f5; max-width: 700px; margin: auto; padding: 20px; }
        #chat { height: 400px; overflow-y: auto; background: white; padding: 15px; border-radius: 10px; margin-bottom: 10px; }
        .user { color: #1a73e8; }
        .ai { color: #d93025; }
        input, button { padding: 10px; font-size: 16px; width: 100%; margin-bottom: 10px; }
        button { background: #1a73e8; color: white; border: none; border-radius: 5px; cursor: pointer; }
    </style>
</head>
<body>
    <h2>🧠 دستیار جامع روانشناسی (آموزشی و خودیاری)</h2>
    <p style="font-size:12px; color:gray;">⚠️ هشدار: این ابزار جایگزین روانشناس نیست. در بحران با ۱۲۳ تماس بگیرید.</p>
    <div id="chat"></div>
    <input type="text" id="userInput" placeholder="سوالت رو اینجا بنویس..." />
    <button onclick="sendMessage()">ارسال</button>

    <script>
        const API_KEY = AQ.Ab8RN6Jdb5m6d1t1bJKmlx4gJH1fQ1fC55jelkT4wCma4Oyh_g
        const chatDiv = document.getElementById("chat");
        const systemPrompt = `تو یک استاد برجسته و همه‌فن‌حریف روانشناسی هستی. دانش کامل و دقیقی در تمام شاخه‌های روانشناسی داری: بالینی (DSM-5)، شناختی، رشد، اجتماعی، شخصیت، عصب‌روانشناسی، مثبت‌گرا، صنعتی-سازمانی. همچنین به تمام رویکردهای درمانی مسلط هستی: CBT، DBT، طرحواره‌درمانی، ACT، روان‌درمانی پویشی، زوج‌درمانی، وجودی.
        وظیفهٔ تو روان‌آموزی و ارائهٔ تکنیک‌های خودیاری مبتنی بر شواهد است. هرگز تشخیص بالینی نده، ولی می‌تونی علائم رو با معیارهای علمی تطبیق بدی و بگی «برای تشخیص دقیق به متخصص مراجعه کن». 
        همیشه پاسخ‌هایت را با مفاهیم علمی از شاخهٔ مربوطه غنی کن. اگر نشانه‌ای از بحران (خودکشی، آسیب به خود یا دیگران) دیدی، فوراً پروتکل ارجاع اضطراری را اجرا کن و شمارهٔ ۱۲۳ را بده.
        حالا با مهربانی و دقت علمی پاسخ بده.`;

        function addMessage(text, sender) {
            const msg = document.createElement("div");
            msg.className = sender;
            msg.innerHTML = "<b>" + (sender === "user" ? "👤 شما" : "🤖 روانشناس") + ":</b> " + text;
            chatDiv.appendChild(msg);
            chatDiv.scrollTop = chatDiv.scrollHeight;
        }

        async function sendMessage() {
            const input = document.getElementById("userInput");
            const userText = input.value.trim();
            if (!userText) return;
            input.value = "";
            addMessage(userText, "user");

            const aiMessage = addMessage("در حال بررسی علمی...", "ai");
            try {
                const response = await fetch(https://generativelanguage.googleapis.com/v1beta/models/gemini-1.5-flash:generateContent?key=${API_KEY}, {
                    method: "POST",
                    headers: { "Content-Type": "application/json" },
                    body: JSON.stringify({
                        system_instruction: { parts: [{ text: systemPrompt }] },
                        contents: [{ parts: [{ text: userText }] }]
                    })
                });
                const data = await response.json();
                const reply = data.candidates[0].content.parts[0].text;
                // حذف پیغام «در حال بررسی»
                chatDiv.removeChild(aiMessage);
                addMessage(reply, "ai");
            } catch (error) {
                chatDiv.removeChild(aiMessage);
                addMessage("خطا در ارتباط. لطفاً اینترنت و کلید API را بررسی کن.", "ai");
                console.error(error);
            }
        }
    </script>
</body>
</html>